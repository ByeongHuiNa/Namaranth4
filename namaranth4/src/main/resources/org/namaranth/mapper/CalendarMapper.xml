<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.namaranth.mapper.CalendarMapper">

	<select id="getList" resultType="org.namaranth.domain.CalendarVO">
		select distinct c.cal_no, c.cal_name, c.cal_color
		from calendar c
		left join cal_parti cp on c.cal_no = cp.cal_no
		where c.user_no = 1 or cp.user_no = 1

	</select>
	
	
	
</mapper>
